import Section from "../components/Section.jsx";

export default function Returns() {
  return (
    <Section
      eyebrow="Returns"
      title="Returns and exchanges"
      lead="This page is ready for the final returns policy before launch."
    >
      <div className="policy-copy">
        <p>Return windows, product condition requirements, and support steps should be added here.</p>
      </div>
    </Section>
  );
}

